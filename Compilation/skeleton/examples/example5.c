int main() {
  int num [7];
  if (num % 2 == 0) {
    // le nombre est pair
  } else {
    // le nombre est impair
  }

  int n [5];
  int fact [1];
  while (n > 0) {
    fact = fact * n;
    n = n - 1;
  }
}